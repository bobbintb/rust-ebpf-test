[package]
name = "dirt-ebpf"
version = "0.1.0"
edition.workspace = true

[dependencies]
aya-ebpf = { workspace = true }

[build-dependencies]
which = { workspace = true }

[lib]
crate-type = ["staticlib"]
path = "src/lib.rs"

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"
